# ChordVox vX.Y.Z

> NOTE: Release notes must be written in Markdown only.
> Do not paste HTML such as `<h1>`, `<ul>`, `<li>`, `<div>`, or GitHub-rendered code block wrappers.
> Keep public notes user-facing. Do not include private repo names, source SHAs, or internal manifest references.

## Downloads

- macOS (Apple Silicon): `ChordVox-X.Y.Z-arm64.dmg`
- macOS (Intel): `ChordVox-X.Y.Z.dmg`
- Windows Installer: `ChordVox.Setup.X.Y.Z.exe`
- Windows Portable: `ChordVox.X.Y.Z.exe`
- Linux AppImage: `ChordVox-X.Y.Z-linux-x86_64.AppImage`
- Linux DEB: `ChordVox-X.Y.Z-linux-amd64.deb`
- Linux RPM: `ChordVox-X.Y.Z-linux-x86_64.rpm`
- Linux tar.gz: `ChordVox-X.Y.Z-linux-x64.tar.gz`

## macOS First Launch

macOS first launch may require permission because the app is distributed outside the App Store. If Gatekeeper blocks the app, run:

```bash
xattr -dr com.apple.quarantine /Applications/ChordVox.app
open /Applications/ChordVox.app
```

## Highlights

- <本次新增功能 1>
- <本次修复 1>
- <本次修复 2>
